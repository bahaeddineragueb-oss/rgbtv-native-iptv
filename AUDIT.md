# تقرير فحص شامل — RGBTv Native IPTV

**تاريخ الفحص:** 2026-09-09 · **الفرع:** `arena/01a08635-rgbtv-native-iptv` · **آخر commit:** `c62f6db`
**نطاق الفحص:** 6 ملفات Kotlin (326 سطراً) + AndroidManifest + الموارد + إعدادات Gradle + الـ APK المرفوع.

> **طريقة الفحص وحدوده:** لا يوجد Android SDK ولا محاكي في بيئة العمل، ولم يُبنَ المشروع (لا يوجد حتى `gradlew`). النتائج مبنية على مراجعة ساكنة للكود، وحساب نسب تباين الألوان فعلياً، ومحاكاة منطق الـ M3U parser ببرنامج مستقل. كل بند مشفوع بموقعه `ملف:سطر` لتتمكن من التحقق منه بنفسك.

---

## ملخص تنفيذي

| الخطورة | العدد | أبرز الأمثلة |
|---|---|---|
| 🔴 حرج | 4 | المُحلِّل يبتلع معظم القنوات · لا توجد أي حالة خطأ/تحميل ظاهرة · الشريط الجانبي ينهار مع البلايليست الحقيقية · تحليل 50MB على الخيط الرئيسي |
| 🟠 عالي | 12 | لا معالجة لأخطاء التشغيل · كلمة المرور نص صريح · البيانات السرية داخل مسار URL · لا RTL ولا تلفاز · لا edge-to-edge |
| 🟡 متوسط | 14 | تباين أبيض/وردي 3.41:1 · الثيم ناقص (Typography/Shapes/أدوار ألوان) · وميض أبيض عند الإقلاع · الأيقونة غير تكيّفية |
| 🔵 منخفض | 11 | تبعيات ميتة · حقول ميتة · لا اختبارات/ـ CI · APK بـ 22MB داخل Git |

**الخلاصة:** المشروع «هيكل أولي» (skeleton) نظيف المظهر في الكود، لكنه **غير صالح للاستخدام مع بلايليست حقيقية** و**لا يعرض أي ملاحظات للمستخدم**: لا تحميل، لا خطأ، لا حالة فارغة، لا مؤشر للقناة المشغّلة. كما أن الهوية البصرية الموصوفة في README («لمسات سماوية»، «طباعة عالية التباين») **غير مطبّقة فعلياً** في الكود.

---

## 1) العيوب الشكلية والبصرية (Visual) — وهو محور طلبك

### 🟠 1.1 الشريط العلوي ينكسر على الشاشات الضيقة — `MainActivity.kt:70`
عرض حقل البحث ثابت `330.dp` يسبقه `Spacer(weight(1f))`. الحساب: شعار (~90) + بحث (330) + فراغ (12) + زر (48) + حشو (48) ≈ **528dp**. على هاتف أفقي بعرض 480–568dp تُدفع العناصر خارج حدود الشاشة (Row لا يقصّها افتراضياً) فيختفي زر الحسابات.
**الإصلاح:** `Modifier.weight(1f).widthIn(max = 330.dp)` بدل العرض الثابت، و`.heightIn(min = 56.dp)` بدل `76.dp` الثابت.

### 🟠 1.2 لا يوجد تمرير عمودي للتخطيط — `MainActivity.kt:48-58`
هرميّة ثابتة: `TopBar(76) + HeroPlayer(330) + Header + LazyRow`. على ارتفاع أفقي صغير (هاتف 360dp) **تُقصّ بطاقات القنوات نهائياً** دون إمكانية التمرير.
**الإصلاح:** `Modifier.verticalScroll(rememberScrollState())` على العمود الأيمن، أو تحويله إلى `LazyColumn` بقسم واحد (sticky header).

### 🟠 1.3 لا `edge-to-edge` ولا معالجة `WindowInsets` — `MainActivity.kt:47`, `themes.xml`
`targetSdk = 35` ⇒ على Android 15+ يفرض النظام edge-to-edge، بينما التطبيق لا يستدعي `enableEdgeToEdge()` ولا يطبّق `safeDrawingPadding`. النتيجة: الشريط العلوي وأول عنصر في الشريط الجانبي يتداخلان مع شريط الحالة/التنقّل، وفي الأفقي يتداخل شريط التنقّل الجانبي مع نهاية صف القنوات.
**الإصلاح:** `enableEdgeToEdge()` في `onCreate` + `windowInsetsPadding(WindowInsets.safeDrawing)` + `imePadding()` في الحوار.

### 🟡 1.4 وميض أبيض عند الإقلاع (cold-start flash) — `themes.xml`
لا يوجد `android:windowBackground`. النظام يرسم خلفية فاتحة/افتراضية ريثما يرسم Compose خلفيته الداكنة ⇒ **وميض أبيض مرئي** عند كل إقلاع — وهي من أوضح العيوب الشكلية.
**الإصلاح:** `<item name="android:windowBackground">@color/…(#080A12)</item>` أو اعتماد `core-splashscreen`.

### 🟡 1.5 الثيم ناقص والهوية غير مكتملة — `Theme.kt:9-25`
- `isSystemInDarkTheme` مستورد (سطر 3) و**غير مستخدم** — الثيم داكن قسراً بلا دعم للوضع الفاتح.
- لا `Typography` ولا `Shapes` ⇒ طباعة M3 الافتراضية، وادعاء «طباعة عالية التباين» في README غير محقّق.
- `darkColorScheme()` يعبّئ الأدوار غير المحدّدة (`error`, `outline`, `primaryContainer`, `surfaceContainer*`, `scrim`) من **خط الأساس البنفسجي الافتراضي للماتيريال** لا من الهوية ⇒ حدود الحقول، وحاوية الحوار، وألوان التحديد قد تظهر بلون بنفسجي/رمادي لا ينتمي لpalette الـ RGB.
- اللون `tertiary = #20D9D2` (سماوي) **غير مستخدم في أي مكان في الواجهة** رغم أن README و NATIVE-MIGRATION يذكران «لمسات سماوية».
**الإصلاح:** تعريف `Typography`/`Shapes` صريح، وتحديد بقية أدوار الألوان (خصوصاً `surfaceContainer*` و`error` و`outline`)، واستخدام السماوي فعلياً (مثلاً مؤشر القناة المشغّلة/شارة LIVE).

### 🟡 1.6 تباين الألوان — `Theme.kt:10-11`
حسابات فعلية (WCAG 2.1):

| الزوج | النسبة | الحكم |
|---|---|---|
| أبيض `#FFFFFF` على `primary` `#FF3D71` (نص زر «Connect») | **3.41:1** | ❌ أقل من 4.5:1 للنص العادي |
| `primary` على الخلفية `#080A12` | 5.80:1 | ✅ |
| `onSurfaceVariant` على `surfaceVariant` | 7.22:1 | ✅ |
| `onSurface` على `surface` | 17.15:1 | ✅ ممتاز |

**الإصلاح:** دكنة `primary` قليلاً (مثلاً `#E01E5A` ⇒ ~4.6:1 مع الأبيض) أو تكبير/تنعيل نص الزر، مع الحفاظ على الوردي في العناصر غير النصية.

### 🟡 1.7 الأيقونة غير تكيّفية ولا يوجد بانر تلفازي — `ic_launcher.xml`
Vector عادي في `res/drawable` بلا `mipmap-anydpi-v26` ولا `background/foreground` ⇒ على لانشر Android 8+ تُقصّ الأيقونة داخل دائرة/شكل اللانشر بشكل غير متوقّع، ولا يوجد `android:roundIcon` ولا `android:banner` (مطلوب للتلفاز).

### 🟡 1.8 الحوار لا يمرّر ولا يتعامل مع لوحة المفاتيح — `MainActivity.kt:77-92`
محتوى الحوار عمود بلا `verticalScroll` وبلا `imePadding`. مع 3 حقول (تبويب Xtream) على شاشة أفقية قصيرة **أو عند فتح لوحة المفاتيح** تُقصّ الحقول السفلية (حقل كلمة المرور تحديداً) ولا يمكن الوصول إليها — وهذا ما حاول commit `c62f6db` معالجته جزئياً عبر `widthIn`.
**أيضاً:** عناوين التبويبات (`maxLines = 1`، سطر 85) بلا `TextOverflow.Ellipsis` ⇒ تُقصّ بلا «…».

### 🟡 1.9 متحكّم ExoPlayer الافتراضي يتعارض مع الهوية — `MainActivity.kt:106`
`PlayerView` الافتراضي يستخدم `exo_player_control_view.xml` (موجود فعلاً داخل الـ APK) بألوان ماتيريال فاتحة/زرقاء، ولا يُضبط `resize_mode` ⇒ تختلف نسبة العرض (letterbox) عن باقي الأسطح المستديرة. كما أن التبعية `media3-ui-compose` مضافة و**غير مستخدمة** — وهي الأنسب لبناء متحكّم بنفس الهوية.

### 🔵 1.10 عيوب شكلية صغيرة
- **ازدواج نص الشعار** (سطر 67-68): مربع مكتوب فيه «RGB» ثم كلمة «RGB» + «Tv» بجانبه.
- **أحجام ثابتة** `76/330/168/220.dp` تنكسر مع «حجم الخط/العرض» الكبير في إعدادات النظام.
- حقل البحث بلا زر مسح، بلا `keyboardActions`/`imeAction`، وبشكل M3 الافتراضي (زوايا صغيرة) لا يتّسق مع زوايا 16–22dp في باقي التصميم.
- لا `@Preview` لأي شاشة رغم وجود `ui-tooling-preview` ⇒ لا يمكن مراجعة الشكل داخل Studio، ولا توجد لقطات شاشة في المستودع.

---

## 2) عيوب تجربة الاستخدام والحالات الناقصة (UX/States)

### 🔴 2.1 لا توجد أي حالة تحميل أو خطأ مرئية — `MainActivity.kt:42`
`vm.loading` لا يُقرأ إطلاقاً، و`playerState` يُستهلك فقط عبر `selectedChannel`؛ حقول `error` و`isBuffering` و`isPlaying` ميتة. وأسوأ من ذلك: زر «Connect» (سطر 92) ينفّذ `onDismiss()` فوراً ⇒ **أي فشل في الاتصال يمرّ بصمت تام**: لا رسالة، لا تراجع، لا إعادة محاولة.

### 🔴 2.2 لا توجد حالة فارغة (Empty State) — `MainActivity.kt:55`
لا شيء يُعرض عند: صفر قنوات، أو بحث بلا نتائج، أو بلايليست فارغة ⇒ مساحة ميتة سوداء بلا تفسير ولا إجراء.

### 🟠 2.3 لا مؤشر للقناة المشغّلة حالياً — `ChannelCard` (سطر 112)
البطاقة لا تعرف حالتها المختارة ⇒ لا إطار/توهّج/شارة «يُشغَّل الآن» على القناة التي تعمل في المشغّل. فجوة بصرية واضحة جداً في مشغّل IPTV.

### 🟠 2.4 المفضلة بلا سطح عرض ولا حفظ — `MainViewModel.kt:102`
`toggleFavorite` تعمل في الذاكرة فقط: لا تبويب «المفضلة» في الشريط الجانبي، ولا حفظ (لا Room) ⇒ تضيع عند إعادة التشغيل وعند تحميل بلايليست جديدة (السطر 37/44 يستبدل القائمة بالكامل).

### 🟠 2.5 حوار الحساب يفتح إجبارياً بلا «تخطّي» وبلا تحقّق — `MainActivity.kt:60, 87-92`
- `showAccounts = true` ابتداءً ⇒ يفتح دائماً ولا يوجد زر إغلاق `X` مرئي (الإغلاق بالخلفية/زر النظام فقط).
- لا تحقّق من الحقول الفارغة ⇒ «Connect» بحقول فارغة يبدأ طلباً وهمياً.
- لا رسالة خطأ داخل الحوار، ولا مؤشر تحميل على الزر.

### 🟡 2.6 حقل كلمة المرور نص صريح — `MainActivity.kt:87`
بلا `visualTransformation = PasswordVisualTransformation()` وبلا `KeyboardOptions(keyboardType = Password, imeAction = Done)` وبلا autofill ⇒ كلمة المرور تظهر لمن يقف خلفك على التلفاز. (هذا عيب أمني وتجربة معاً.)

### 🟡 2.7 القياسات اللمسية/عن بُعد — `ChannelCard` (سطر 112)
زر المفضلة `size(30.dp)` أقل بكثير من الحد الأدنى 48dp ⇒ صعب بالريموت واللمس. ولا يوجد أي `focusable()`/إبراز تركيز لأي عنصر.

---

## 3) أخطاء وظيفية (Bugs)

### 🔴 3.1 مُحلِّل M3U يفقد القنوات التي بلا سمات — `M3uParser.kt:20`
الشرط `!line.startsWith("#") && metadata.isNotEmpty()` يعني أن أي `#EXTINF:-1,Channel Name` **بلا سمات** تُترك قناته (السطر التالي) مهملة، لأن `metadata` يبقى فارغاً.
محاكاة فعلية للمنطق على بلايليست من قناتين (واحدة بلا سمات والأخرى بسمات): **أُرجعت قناة واحدة فقط** — وهي ذات السمات. أي أن شريحة كبيرة من بلايليستات M3U البسيطة (وهي الشائعة) تُعطي **قائمة فارغة تماماً**.
**الإصلاح:** تتبّع «رأينا `#EXTINF`» بمتغيّر منطقي منفصل بدل الاعتماد على `metadata.isNotEmpty()`.

### 🔴 3.2 الشريط الجانبي ليس Lazy ولا يمرّر — `MainActivity.kt:97`
`groups.forEach { … }` داخل `Column` عادي: كل التصنيفات تُرسم دفعة واحدة. بلايليست واقعية بـ 200–2000 تصنيف ⇒ **تجمّد/انهيار**، وما فوق الشاشة لا يمكن الوصول إليه أصلاً. **الإصلاح:** `LazyColumn` + `verticalScroll` كحد أدنى + بحث/طيّ للتصنيفات.

### 🔴 3.3 تحليل البلايليست على الخيط الرئيسي — `MainViewModel.kt:41`
`loadM3uText(withContext(IO){ openText(...) })`: التنزيل على IO ثم **التحليل يعود على Main** (لأن `loadM3uText` يُستدعى من كوروتين `viewModelScope` على Main). مع حدّ مسموح يصل إلى **50MB** (`M3uParser.kt:7`) و`toList()` لكل الأسطر ⇒ ANR وOOM شبه مؤكدان.
**الإصلاح:** `withContext(Dispatchers.Default) { parse(text) }` + بثّ (streaming) بدل `toList()` + حدّ أقل (مثلاً 20MB).

### 🟠 3.4 لا مستمع لأخطاء المشغّل، والبث يستمر بعد إلغاء التحديد — `MainActivity.kt:104-106`
- لا `Player.Listener` ⇒ أي رابط ميت يبقى «شاشة سوداء + تحميل لانهائي» بلا رسالة.
- `LaunchedEffect(channel?.streamUrl)`: عند `channel == null` لا يُستدعى `stop()` ⇒ **الصوت يستمر** خلف طبقة «Select a channel».
- لا `pause()/play()` في `onStop/onStart` ⇒ التشغيل يستمر في الخلفية.
- لا `MediaSession`، لا PiP (ولا `supportsPictureInPicture` في المانيفست)، لا audio focus، لا `keepScreenOn`.

### 🟠 3.5 لا فحص لرمز استجابة HTTP — `MainViewModel.kt:99`
`openText` يقرأ `inputStream` مباشرة: استجابة 401/404/500 (أو صفحة خطأ HTML) تُرمى كـ `FileNotFoundException` أو `JSONException` ويُعرض نصّها الخام للمستخدم (`MainViewModel.kt:54`) ⇒ **رسائل مضلّلة + تسريب لبيانات الاعتماد** (راجع 4.2). كما لا يوجد `disconnect()`، ولا User-Agent (بعض المزودين يرفضون UA الافتراضي `Dalvik`)، ولا معالجة لإعادة التوجيه بين http↔https (شائعة في بوابات Stalker).

### 🟡 3.6 أخطاء أخرى
- `MainViewModel.kt:69`: اسم المستخدم وكلمة المرور داخل مسار الرابط `$server/live/$user/$pass/$id.m3u8` **غير مُرمَّزين** (عكس السطر 61 الذي يرمّزهما) ⇒ ينكسران مع الرموز الخاصة.
- `MainViewModel.kt:75`: `uppercase()` باللغة الافتراضية (مشكلة التركية مع `i`)؛ ولا فلترة إدخال لحقل MAC، ولا ترميز تلقائي أثناء الكتابة.
- `MainViewModel.kt:41`: `loadM3uUrl` يعالج الخطأ مرتين (داخلياً عبر `loadM3uText` وخارجياً في `launchLoad`) ⇒ تكرار وتضارب في الرسائل.
- `MainActivity.kt:44-45`: `groups` و`filtered` يُحسبان في كل إعادة تركيب (مع كل حرف بحث) بلا `remember`/`derivedStateOf` ⇒ O(n) متكرر.
- `MainActivity.kt:46`: `loadDemoCatalog()` يُستدعى دائماً ⇒ بيانات تجريبية تظهر قبل/فوق أي مصدر حقيقي في بعض المسارات.
- `SecureCredentials.kt` **غير مستخدم إطلاقاً** ⇒ البنية التحتية المشفّرة ميتة، وبيانات الاعتماد لا تُحفظ.
- الكود يستخدم `HttpURLConnection` بينما OkHttp مضاف كتبعية وغير مستخدم ⇒ تضارب في أسلوب الشبكة.

---

## 4) الأمان والخصوصية

| # | الخطورة | المشكلة | الموقع |
|---|---|---|---|
| 4.1 | 🟠 | `usesCleartextTraffic="true"` **على مستوى التطبيق بالكامل**، و`normalize()` (سطر 96) يفترض `http://` افتراضياً ⇒ بيانات الاعتماد ترسل نصّاً صريحاً إن نسي المستخدم كتابة `https://` | AndroidManifest |
| 4.2 | 🟠 | كلمة المرور داخل **مسار URL** في روابط Xtream (سطر 69) ⇒ تُسجَّل في البروكسيات والسجلات؛ ورسائل الخطأ (`it.message`) تعرض الرابط كاملاً بالمستخدم وكلمة المرور | `MainViewModel.kt:69, 54` |
| 4.3 | 🟠 | كلمة المرور معروضة كنص صريح في الحوار (بلا `PasswordVisualTransformation`) | `MainActivity.kt:87` |
| 4.4 | 🟡 | `security-crypto:1.1.0-alpha06` (إصدار alpha) في مشروع يعلن الجاهزية؛ و`SecureCredentials` ينفّذ عملاً ثقيلاً وقد يرمى على الخيط الرئيسي إن استُخدم | `build.gradle.kts`, `SecureCredentials.kt` |
| 4.5 | 🔵 | `-keep class com.rgbtv.app.** { *; }` يفرّغ `isMinifyEnabled = true` من معناه لكود التطبيق | `proguard-rules.pro` |

✅ **إيجابي:** `android:allowBackup="false"` (يمنع تسريب البيانات عبر النسخ الاحتياطي)، ولا توجد أسرار مُضمَّنة بشكل ثابت في المستودع (فحصتُ: لا كلمات مرور ولا MAC ولا توكنز).

---

## 5) التلفاز والوصولية والعولمة (ادعاءات README غير محقّقة)

### 🟠 5.1 التطبيق لن يظهر أصلاً على لانشر Android TV — `AndroidManifest.xml`
رغم أن README يعلن «واجهة موجهة للتلفاز»: لا `<uses-feature android:name="android.software.leanback" android:required="false">`، ولا `android.intent.category.LEANBACK_LAUNCHER`، ولا `android:banner`.

### 🟠 5.2 لا دعم لريموت الـ D-pad
لا `focusable()` ولا إبراز تركيز ولا `focusableItem` على أي عنصر (الشريط الجانبي/البطاقات/الحقول) ⇒ التنقّل بالريموت غير ممكن عملياً. المكتبة الأنسب `androidx.tv:tv-material` غير مضافة.

### 🟠 5.3 لا RTL ولا تدويل — `strings.xml`, `MainActivity.kt`
ملف النصوص يحتوي **سطراً واحداً** (`app_name`) وكل نصوص الواجهة مُضمَّنة بشكل ثابت بالإنجليزية داخل Kotlin، ولا `android:supportsRtl="true"` ⇒ لا عربية ولا RTL ولا ترجمة ممكنة دون إعادة كتابة.

### 🟡 5.4 `screenOrientation="sensorLandscape"` قسري
يُجبر الهواتف على الأفقي (سلوك عدائي على هاتف محمول باليد)، بينما الهوية لا تستفيد فعلياً من المساحة الأفقية (الشريط الجانبي 220dp ثابتة، صف أفقي واحد للقنوات).

### 🔵 5.5 صلاحية `ACCESS_NETWORK_STATE` معلنة وغير مستخدمة.

---

## 6) البناء وجودة المستودع (Repo Hygiene)

| # | الخطورة | المشكلة |
|---|---|---|
| 6.1 | 🟠 | **لا يوجد `gradlew`/`gradle-wrapper.jar`** ⇒ لا يمكن بناء المشروع في CI أو من نسخة جديدة (README يعترف بذلك). أضِف الـ wrapper فعلياً. |
| 6.2 | 🟠 | **APK بحجم 22MB مرفوع داخل Git** (حجم الحزمة الكاملة 20.6MB، أي ~99% من تاريخ المستودع). انقله إلى GitHub Releases وأزِله من التاريخ (`git rm --cached` + `git filter-repo`/BFG). |
| 6.3 | 🟡 | لا يوجد CI إطلاقاً (لا `.github/workflows`)، ولا اختبارات (لا `testImplementation` سوى BOM)، ولا ktlint/detekt. |
| 6.4 | 🟡 | **5 تبعيات ميتة** تضخّم APK الـ debug (8 ملفات dex، `classes.dex` وحده 44MB): `media3-session`، `media3-ui-compose`، `navigation-compose`، `coil-compose` (الشعارات لا تُعرض أصلاً!)، `material-icons-extended` (مقابل 6 أيقونات فقط) + `core-ktx` غالباً غير مستخدم. |
| 6.5 | 🔵 | لا version catalog (`libs.versions.toml`)، لا `composeOptions`، `enableJetifier=true` بلا داعٍ في 2026. |
| 6.6 | 🔵 | `collectAsState` بدل `collectAsStateWithLifecycle` (التبعية مضافة وغير مستفادة). |
| 6.7 | 🔵 | حقل `Channel.logo` مُحلَّل ومخزّن ولا يُعرض في أي مكان؛ و`PlayerState.isPlaying/isBuffering` وحقول `Models.Profile` ميتة. |

---

## 7) خطة إصلاح مقترحة (مراحل)

**المرحلة 1 — جعل التطبيق صالحاً للاستخدام (يوم 1)**
1. إصلاح `M3uParser` (قبول القنوات بلا سمات) + نقل التحليل إلى `Dispatchers.Default`.
2. `LazyColumn` للشريط الجانبي + تمرير عمودي للتخطيط.
3. عرض حالات: تحميل (شريط/هيكل عظمي)، خطأ (snackbar/بطاقة مع إعادة محاولة)، فارغ (لا نتائج/لا قنوات).
4. مستمع `Player.Listener` + إيقاف عند `channel == null` + ربط `onStop/onStart`.
5. `PasswordVisualTransformation` + تحقّق الحقول + عدم إغلاق الحوار قبل نجاح التحميل + `imePadding`/`verticalScroll`.
6. إبراز القناة المشغّلة حالياً + حالة عدم وجود نتائج.

**المرحلة 2 — الشكل والهوية (يوم 2–3)**
7. `edge-to-edge` + `WindowInsets` + `windowBackground` داكن (إزالة الوميض الأبيض).
8. إكمال الثيم: `Typography` + `Shapes` + بقية أدوار الألوان + استخدام السماوي (`tertiary`) فعلياً.
9. ضبط `primary` لرفع التباين إلى ≥4.5:1 على الأبيض.
10. حقل بحث مرن (`weight` + `widthIn`) وزر مسح و`imeAction`.
11. أيقونة تكيّفية (`mipmap-anydpi-v26`) + `roundIcon` + `banner` للتلفاز.
12. متحكّم مشغّل بهوية RGB (عبر `media3-ui-compose`) أو تنسيق `exo_player_control_view`.
13. `@Preview` لكل شاشة + لقطات شاشة في README.

**المرحلة 3 — الإنتاج (أسبوع)**
14. Gradle wrapper + CI (build + lint + unit tests) + إزالة APK من Git.
15. Room للملفات الشخصية/المفضلة/السجل + استخدام `SecureCredentials` فعلياً + تخزين مشفّر.
16. MediaSession + PiP + audio focus + `LEANBACK_LAUNCHER` + دعم D-pad.
17. نصوص في `strings.xml` + `supportsRtl="true"` + عربية/RTL.
18. OkHttp لكل الشبكة + User-Agent + فحص رموز الاستجابة + عدم تسريب بيانات الاعتماد في الرسائل + تفضيل HTTPS.
19. حذف التبعيات الميتة وتقليل حجم APK.

---

## 8) ما لم أتمكّن من التحقق منه

- **لم يُبنَ المشروع ولم يُشغَّل على جهاز/محاكي** (لا Android SDK في البيئة): كل العيوب البصرية أعلاه مستنتَجة من الكود والموارد وحسابات التباين، وتحتاج تأكيداً بصرياً على جهاز حقيقي بعد الإصلاح.
- **لم أفكّ ترميز الـ APK** (لا `aapt`/`apkanalyzer`): حلّلتُ محتوياته المضغوطة فقط (وجود موارد متحكّم ExoPlayer، 8 ملفات dex، لا يوجد توقيع إصدار).
- سلوك المزودين الحقيقيين (Xtream/Stalker) لم يُختبر — لا بيانات اختبار متاحة.

---

## 9) سجل الإصلاحات المطبّقة (2026-09-09)

### ✅ تم إصلاحه

| البند | الحالة |
|---|---|
| 3.1 مُحلِّل M3U يفقد القنوات | أُعيدت كتابة `M3uParser`: فصل «رأينا #EXTINF» عن خريطة السمات، والفصل على أول فاصلة **خارج علامات التنصيص** (للأسماء التي تحتوي فواصل)، ودعم `#EXTGRP` قبل/بعد `#EXTINF`، وأسطر URL بلا `#EXTINF`، واستخراج اسم من المسار. + 6 اختبارات وحدة في `M3uParserTest`. |
| 3.3 التحليل على الخيط الرئيسي | التنزيل على `Dispatchers.IO` والتحليل على `Dispatchers.Default`، ورفض القوائم > 25MB قبل التحليل. |
| 2.1 لا حالات تحميل/خطأ | `loading` + `UiMessage` (معرّف نص + وسائط) تُعرض كشريط تنبيه مع زر «إعادة المحاولة»، وحالة تحميل، وحالات فارغة (لا قنوات/لا نتائج/لا مفضلة)، وأخطاء الحقول تُعرض **داخل الحوار**. |
| 3.4 أخطاء المشغّل | `Player.Listener` + تراكب خطأ مع «إعادة المحاولة»، وإيقاف عند `channel == null`، وإيقاف/استئناف مع دورة الحياة (`ON_STOP/ON_START`)، و`keepScreenOn`. |
| 2.6 كلمة المرور نص صريح | `PasswordVisualTransformation` + زر إظهار/إخفاء + `KeyboardType.Password`. |
| 4.2/4.3 بيانات الاعتماد في الروابط والرسائل | ترميز المستخدم/كلمة المرور في مسار Xtream، ورسائل الخطأ تعتمد معرّفات نصوص (`UiMessage`) فلا يُعرض رابط المزوّد ولا بيانات الدخول أبداً. |
| 3.5 رموز استجابة HTTP | فحص `responseCode`، و`User-Agent` ثابت، و`disconnect()` في `finally`، و`finally` لإلغاء حالة التحميل، وإعادة رمي `CancellationException`. |
| 3.2 الشريط الجانبي غير Lazy | `LazyColumn` + تمرير + تمرير تلقائي إلى العنصر المُركَّز (ريموت). |
| 2.3 لا مؤشر للقناة المشغّلة | حدود وإبراز للبطاقة المختارة + شريط «مباشر» مع اسم القناة وأزرار تشغيل/إيقاف داخل المشغّل. |
| 2.4 المفضلة بلا سطح عرض | زر تصفية المفضلة في الشريط العلوي + حالة فارغة مخصّصة. |
| 1.1/1.2 التخطيط | شريط علوي مرن (`weight` + `widthIn`)، وتخطيط متكيّف: شريط تصنيفات رأسي للتابلت/التلفاز، وشرائح أفقية + شبكة للهاتف، وارتفاع المشغّل نسبةً من الشاشة. |
| 1.3 edge-to-edge | `enableEdgeToEdge()` + `windowInsetsPadding(safeDrawing)` + `imePadding` في الحوار. |
| 1.4 الوميض الأبيض | `android:windowBackground` بلون الكانفس. |
| 1.5 الثيم الناقص | `Typography` + `Shapes` + كل أدوار الألوان (`surfaceContainer*`, `error*`, `outline`, `scrim`) + استخدام السماوي (`tertiary`) في شارة «مباشر» وإطار التركيز. |
| 1.6 التباين | فصل الوردي: `#FF3D71` للنص/الأيقونات (5.8:1) و`#D81B60` للأسطح الممتلئة خلف نص أبيض (4.95:1 بدل 3.41:1). |
| 1.7 الأيقونة | أيقونة تكيّفية (`mipmap-anydpi-v26` + طبقة `monochrome` في `-v33`) + بانر تلفازي `tv_banner`. |
| 1.9 متحكّم ExoPlayer | `useController = false` + متحكّم Compose بنفس الهوية. |
| 5.1/5.2 التلفاز | `LEANBACK_LAUNCHER` + `banner` + `uses-feature leanback/touchscreen required=false` + `supportsPictureInPicture` + إبراز تركيز D-pad وتمرير تلقائي وأهداف 48dp. |
| 5.3 RTL والتدويل | كل النصوص إلى `strings.xml` + ترجمة عربية كاملة في `values-ar` + `supportsRtl="true"` + استخدام Start/End فقط. |
| 5.4 الدوران القسري | حُذف `screenOrientation`، والتخطيط يعمل بالطولي والعرضي. |
| 6.4 التبعيات الميتة | حُذف `navigation-compose` و`media3-session` و`media3-ui-compose` و`okhttp` و`core-ktx`؛ و`coil` صار **مستخدماً** (عرض الشعارات). |
| 6.5 `proguard-rules` | حُذف `-keep class com.rgbtv.app.**` الذي كان يفرّغ التصغير من معناه. |
| 6.1/6.3 البناء | `gradlew` + `gradlew.bat` + `gradle-wrapper.properties` + workflow CI (بناء + اختبارات وحدة + lint تقريبي). |
| 6.2 الـ APK في Git | `git rm --cached` + `*.apk` في `.gitignore`. |
| 1.10 أخرى | `@Preview` لكل المكوّنات، وإزالة `ACCESS_NETWORK_STATE` غير المستخدمة، و`collectAsStateWithLifecycle` بدل `collectAsState`. |

### ⏳ مؤجَّل (يحتاج قرارًا أو عملًا أكبر)

- **تخزين بيانات الاعتماد:** `SecureCredentials` و`security-crypto` ما زالا غير موصولين؛ التوظيف الصحيح يحتاج Room للملفات الشخصية (المرحلة 3). لم أحذف الكود الأصلي.
- **OkHttp:** حُذف لأنه غير مستخدم؛ أعِد إضافته عند نقل الشبكة إلى طبقة repository في المرحلة 3 (مع إلغاء حقيقي للطلبات).
- **تنظيف تاريخ Git من الـ APK:** الـ APK موجود في commit `c62f6db` على `main`، وإزالته من التاريخ تتطلب `git filter-repo`/BFG وإعادة كتابة الفرع الرئيسي — قرار يحتاج تنسيقًا معك.
- **MediaSession / PiP / EPG / Room / التوقيع:** كما في «المرحلة 3» أعلاه.

### ⚠️ غير مُتحقَّق منه

- **لم يُبنَ المشروع:** لا يوجد Android SDK ولا JDK في بيئة العمل، فلا ضمان بعدم وجود أخطاء ترجمة طفيفة (أسماء معاملات/استيرادات). راجعتُ كل ملف يدويًا وفحصتُ توازن الأقواس آليًا، لكن أول `./gradlew assembleDebug` عندك هو الحكم.
- **`gradle-wrapper.jar` غير موجود** في المستودع (لا يمكن تنزيله بلا إنترنت): ولّده مرة واحدة بـ `gradle wrapper --gradle-version 8.10.2` أو بفتح المشروع في Android Studio. الـ CI لا يعتمد عليه (يستخدم صورة `gradle:8.10.2-jdk17`).
- **CI لم يُشغَّل:** ملف الـ workflow مكتوب على الأسلوب القياسي لكنه لم يُختبر على GitHub Actions.
- **رفع الـ APK إلى Releases:** الشبكة محجوبة عن `uploads.github.com` من بيئة العمل؛ نفّذ محليًا:
  ```bash
  gh release create v2.0.0 RGBTv-with-accounts-debug.apk --title "RGBTv 2.0.0 (legacy debug build)" --notes "Pre-audit debug build (commit c62f6db)."
  ```

---

## 10) إضافة لاحقة — ما أُكمل في الجولة الثانية

| البند | الحالة |
|---|---|
| حفظ المصادر | `ProfileStore` (SharedPreferences + `org.json`) يحفظ كل مصدر (Xtream/Stalker/M3U) مع المصدر النشط، ويستعيده تلقائيًا عند الإقلاع. |
| حفظ بيانات الدخول | `SecureCredentials` **موصول أخيرًا**: اسم المستخدم وكلمة المرور تُخزَّن مشفّرة (Keystore/AES-GCM)، وتُنشأ بكسلٍ وبمؤشر ترابط في الخلفية مع `runCatching` حتى لا ي crash التطبيق إن فشل الـ Keystore. |
| حفظ المفضلة | تُطبَّق على القنوات بعد كل تحميل (المعرّفات ثابتة لكل مصدر) وتُحفظ فور التبديل. |
| واجهة المصادر | قائمة «المصادر المحفوظة» داخل الحوار مع لمسة للاتصال وزر حذف. |
| PiP | `onUserLeaveHint` يُدخل وضع الصورة-في-الصورة أثناء التشغيل (المانيفست مجهّز مسبقًا). |
| MediaSession | جلسة Media3 تربط المشغّل بأزرار الوسائط وريموت التلفاز وBluetooth. |
| الإقلاع | الحوار لم يعد يفتح قسريًا: يظهر فقط إن لم يكن هناك مصدر محفوظ؛ وإلا تُستعاد القائمة تلقائيًا. |

## 11) لماذا لا يوجد APK مرفق (حالة البناء)

حاولتُ بناء APK فعلي وإرساله، وهذه نتيجة الفحص الموثّقة:

| المسار | النتيجة |
|---|---|
| بناء محلي | **مستحيل**: لا JDK ولا Android SDK، والمضيفات المطلوبة محجوبة شبكيًا — `repo1.maven.org` (000)، `dl.google.com` (000)، `maven.google.com` (000)، `plugins.gradle.org` (000)، `services.gradle.org` (000)، `deb.debian.org` (000). المسموح فقط: `api/github.com` وnpm وPyPI. بدون Maven لا يمكن تنزيل أي تبعية (AndroidX/Compose/Media3/Coil/Kotlin). |
| GitHub Actions | الملف جاهز ويعمل تلقائيًا عند كل push، لكن آخر تشغيل فشل بخطاب من GitHub: *"The job was not started because recent account payments have failed or your spending limit needs to be increased"* — أي مشكلة فوترة/حد إنفاق على الحساب، لا علاقة لها بالكود. |
| تنزيل السجلات/ال artifacts | `results-receiver.actions.githubusercontent.com` و`objects.githubusercontent.com` محجوبان من البيئة، فلا أستطيع قراءة سجلات التشغيل ولا تنزيل الـ artifact. |

**الخلاصة:** أول بناء ناجح يجب أن يحدث على جهازك أو بعد تفعيل الفوترة في GitHub. الأوامر جاهزة في قسم البناء بالـ README.
